// Write your function here
/*
La función Math.pow() retorna la base elevada al exponente, 
es decir, (base exponente). La base y el exponente estan en el 
sistema numérico decimal.

*/
function square(num){
    return Math.pow(num, 2);
}
console.log(square(5));